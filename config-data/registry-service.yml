server:
  port: 8099

eureka:
  instance:
    hostname: ec2-54-221-112-225.compute-1.amazonaws.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/